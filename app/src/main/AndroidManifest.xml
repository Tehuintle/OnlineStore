<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.com.elektra.bancadigital.elektra">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.BadApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- MODULE LOGIN -->
        <activity
            android:name=".business.login.view.LogInActivity"
            android:theme="@style/ThemeFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".business.login.view.SignUp">
    </activity>

        <activity android:name=".business.products.view.ProductsActivity" />

        <activity
            android:name=".business.products.view.ProductDetailActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

    </application>
</manifest>
